{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check:jsonld": "node scripts/check-jsonld.mjs",
    "build": "node scripts/patch-event-data.mjs && node scripts/fix-events-page.mjs && node scripts/check-jsonld.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
